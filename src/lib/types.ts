export type MenuItem = {
	title: string;
	slug: string;
};

export type Post = {
	title: string;
	link: string;
	source: string;
	imageUrl: string;
	publishDate: string;
};

export type Volunteer = {
	firstName: string;
	lastName: string;
	email: string;
	phone: string;
	shirtSize: string;
};
