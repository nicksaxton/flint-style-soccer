export type MenuItem = {
	title: string;
	slug: string;
};

export type Page = {
	content: any[];
};

export type Post = {
	title: string;
	link: string;
	source: string;
	imageUrl: string;
	publishDate: string;
};
